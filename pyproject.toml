[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Skarf Docs"
authors = [{name = "Wooferz", email = "wooferz.contact@gmail.com"}]
dynamic = ["version", "description"]